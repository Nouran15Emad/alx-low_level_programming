#include <stdio.h>
#include "main.h"

/**
*main - calls func
*
*Return: Always 0 (Sucess)
*/

int main(void)
{
	print_triangle(2);
	print_triangle(10);
	print_triangle(1);
	print_triangle(0);
	return (0);
}
