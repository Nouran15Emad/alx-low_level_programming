#include <stdio.h>
#include "main.h"

/**
*main - calls funcs
*
*Return: Always 0 (Sucess)
*/

int main(void)
{
	print_diagonal(0);
	print_diagonal(2);
	print_diagonal(10);
	print_diagonal(-4);
	return (0);
}
